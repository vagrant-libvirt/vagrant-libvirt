name: Publish Docs
on:
  push:
    branches:
      - main
    paths:
      - 'docs/**'
      - .github/workflows/publish-documentation.yml

permissions:
  contents: write

jobs:
  build-and-deploy:
    uses: ./.github/workflows/deploy-docs.yml
